<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>TaskFlow — Organize Your Work</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 font-sans">
<header class="bg-white shadow-md">
  <div class="container mx-auto flex justify-between items-center py-4 px-6">
    <!-- Logo -->
    <a href="/" class="text-2xl font-bold text-gray-800">TaskMaster</a>

    <!-- Nav Links -->
    <nav class="flex items-center space-x-6">
      <a href="/" class="text-gray-600 hover:text-blue-500">Home</a>

<% if (!user) { %>
  <!-- Show if NOT logged in -->
  <a href="/login" class="text-gray-600 hover:text-blue-500">Login</a>
  <a href="/signup" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Sign Up</a>
<% } else { %>
  <!-- Show if logged in -->
  <a href="/tasks" class="text-gray-600 hover:text-blue-500">Tasks</a>
  <a href="/tasks/add" class="font-semibold text-white bg-gradient-to-r from-indigo-500 to-purple-500 px-4 py-2 rounded hover:bg-green-600">Add New Task</a>
  <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Logout</a>
<% } %>

    </nav>
  </div>
</header>

  <main>
